TODO
This is immediate stuff only for this iteration (1.01) of 41 hours.

$Id$

Unit tester
-----------
Keep original raw URL for as long as possible (3).
Allow expectation to be passed to assertError() method (2).
Switch to a run_me_if_no_runner script (2).
Fix GroupTest test case count being wrong after addTestFile() (3).

Reporter
--------

Mock objects
------------
Change semantics of expectAtLeastOnce() for Jason (2).
Add expectOrder() for loose order checking (3).
Add expectSequence() or strict order checking (3).

Parser
------

Browser
-------
Allow listing of all URLs (4).
Add file uploading (8).
Simple click() method for links, buttons or images (2).
setCookie() should be able to create a new one (1).
Simultaneous POST and GET parameters (3).
Fix split bug with simple URLs not being parsed correctly (1/1) - done.

Web tester
----------
Allow expectations to be used for comparisons (2).
Add assertSubmit() test (1).

Documentation
-------------
Note that additional parameters can be added to get(), post(), etc (2).

Build
-----

